import React from 'react';

const App = () => {
  return <h1>Hello world</h1>;
};

// the commented function below will emit type-error (noImplicitAny) when you run commands "npm run tscheck"
// function fn(s) {
//   console.log(s);
// }

export default App;
